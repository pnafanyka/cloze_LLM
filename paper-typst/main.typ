#align(center)[
  #text(17pt, [Cloze Task: Human vs LLM Predictions])

  #grid(columns: (1fr,) * 1, row-gutter: 24pt)[
    Your name #footnote[Correspondence to #link("mailto:example@example.org")]
  ]

  #datetime.today().display()
]

#set heading(numbering: "1.")

= Introduction #emoji.book

This document reports results of comparing human cloze-task responses
with GPT-4o-mini predictions on Russian text.

= Methods

= Results

= Discussion
